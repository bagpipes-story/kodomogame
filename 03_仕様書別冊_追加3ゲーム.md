# 仕様書・別冊：追加3ゲーム（ボールめいろ／ころころキャッチ／すうじフラッシュ）

- 文書バージョン: v1.0
- 作成日: 2026-08-30
- 前提文書: 01_要件定義書.md v1.2、02_仕様書.md v1.2（共通UI・知育共通機能・性能ガードレールはすべて本書にも適用）
- 想定読者: Claude Code（実装担当AI）

> 本書は追加分のみを定義する別冊。既存文書の書き換えは §7「既存文書への差分適用表」に従うこと。これで収録ゲームは**計10本**になる。

---

## 1. 追加ゲームの概要と知育マッピング

| ゲーム | 元にした遊び | 形式 | 5つのちから |
|---|---|---|---|
| A. ボールめいろ | 実物の「ころころ迷路」玩具（箱を両手で傾けて玉を転がす） | ひとり | ④かたち（空間認識・ルート探索）、⑤こころ（そーっと動かす力の調整・リトライ） |
| B. ころころキャッチ | シーソー板の玉転がし玩具 | ひとり＋交互スコア | ②かんがえる（先の板を準備するプランニング・因果関係）、④かたち（協応） |
| C. すうじフラッシュ | 瞬間記憶（チンパンジーテスト式） | ひとり＋交互スコア | ①おぼえる（視空間ワーキングメモリ）、③かず（1→9の順序） |

- 3本ともCPU思考なし（cpu.jsを作らない）。要件定義書§4.3の例外リストに もぐらたたき・バランスゲームと並べて追加する。
- A・Bは物理演算にMatter.jsを再利用（バランスゲームと同じローカル同梱ライブラリ。新規ライブラリ追加なし）。

### 本書で確定した設計解釈（変更したい場合はここだけ直せばよい）

1. **ボールめいろの操作は「iPhone本体を両手で傾ける」**（実物玩具と同じ体験）。端末の傾きセンサーを使う。
2. **ころころキャッチは「駄菓子屋のシーソーゲーム型」**とする（v1.1で変更）。盤ぜんたいを傾けて、互い違いの段（坂・波波）をジグザグに転がし下ろす実物玩具の再現。操作は**指を画面上で左右に動かすと盤が傾く**（端末傾きはボールめいろ専用のため使わない）。難易度は段の形で出す: かんたん=まっすぐな坂／ふつう=ゆるい波波／むずかしい=大きい波波（傾けるだけでは波を越えられず、揺らして勢いをつける技が必要）。スコアはゴールまでのタイム（失敗なし）。※初版の「板を1枚ずつタップで反転」案はv0.10で実装したが、発案者の意図（実物のシーソーゲーム）と異なったためv0.10.1で本形式に変更した。
3. **すうじフラッシュは「数字がパッと出て隠れる→1から順にタップ」のチンパンジーテスト式**とする。「見た数字を入力する」式より、4歳でも数字を読めれば遊べて、視空間記憶＋数の順序の両方を鍛えられるため。

## 2. 共通の新規モジュール: motion.js（傾きセンサー管理）

ボールめいろ専用だが、将来の再利用を見込み共通モジュールとして js/ 直下に置く。

- **許可フロー（iOS必須要件・2026年時点）**:
  - iOS Safariでは `DeviceOrientationEvent.requestPermission()` を**ボタンタップ等のユーザー操作起点でのみ**呼べる（ページ読み込み時の自動呼び出しはNotAllowedError）。HTTPS必須（GitHub Pagesは対応済み）。
  - 実装: ゲーム開始画面の「はじめる」タップ→ `requestPermission()` → granted なら `deviceorientation` イベント購読。
  - `typeof DeviceOrientationEvent.requestPermission !== "function"` の環境（Android等）は許可不要でそのまま購読。
- **拒否・非対応時のフォールバック**: 「ゆびモード」に自動切替（画面をドラッグした方向に盤が傾く仮想操作）。子どもが誤って「許可しない」を押しても遊べなくならないこと。保護者画面に「もう一度許可する方法（Safariの設定→再読み込み）」を記載。
- **信号処理**:
  - `beta`（前後）と `gamma`（左右）を使用。縦持ち前提。
  - ローパスフィルタ（係数0.2程度）で手ぶれを平滑化。
  - 傾き→重力変換は最大±20°でクランプ（それ以上傾けても速度が上がらない＝激しく振っても意味がない設計。乱暴操作の抑制）。
  - **キャリブレーション**: ラウンド開始時に「iPhoneを たいらに もってね」→ タップした瞬間の姿勢をゼロ点にする（ソファで寝転んで遊んでも成立させるため）。
- **画面回転対策**: manifest/CSSでポートレート固定済みだが、Safariブラウザ遊びでは回転しうるため `screen.orientation` を確認しorientationに応じてbeta/gammaの軸を入れ替える。

## 3. ゲームA: ボールめいろ

- 見た目: 木箱風の盤面を上から見た2D表現。ボールはパチンコ玉風（銀色グラデーションの円）。スタート=みどりのマット、ゴール=旗つきの穴。
- 操作: iPhoneを両手で持って傾ける（motion.js経由）。ボールが実物玩具のように転がる。
- 物理: Matter.js。`engine.gravity.x/y` を毎フレームtiltから設定。壁=静的ボディ（グリッド線分から生成）。ボールは反発低め（restitution 0.2）・摩擦あり（転がり感）。描画はCanvas。
- **迷路データ**: プリセットJSON（手作りで品質担保）。グリッド形式で壁・穴・スタート・ゴールを定義し、難易度ごとに5面をローテーション。

```json
// assets/mazes/normal-01.json の例（0=通路, 1=壁, S=スタート, G=ゴール, H=穴）
{ "grid": ["S1000000", "01011110", "0100H000", "011101G1", ...] }
```

- 難易度:
  - かんたん: 6×9グリッド・通路広め・**穴なし**（4歳が確実にゴールできる）
  - ふつう: 8×12・穴2〜3個
  - むずかしい: 10×14・穴多め・通路狭め・クリアタイム表示
- 穴に落ちたら: 吸い込まれるアニメ＋効果音→「あれれ〜！」→ボールがスタートに戻る。**残機・ゲームオーバーなし**、何度でも再開（落ちた回数だけ記録）。
- クリア: ゴール穴に入ると「ぽとん！」→紙吹雪。星評価=穴に落ちた回数（0回=星3、1〜2回=星2、3回以上=星1）。むずかしいのみタイムも表示。
- **知育要素**:
  - プレイ前に2秒間、スタート→ゴールの推奨ルートがうっすら光る「めで たどってみよう」演出（ON/OFF可）→ 目でルートを追う先行プランニング
  - そーっと傾けないと穴に落ちる=力の調整（graded control）。乱暴に振っても速くならないクランプ設計とセット
  - ほめイベント: 穴0回クリア `no_fall_clear`（「そーっとうごかせたね！」）、落ちても再挑戦 `retried`
  - 保護者向けヒント: 「実物の木の迷路玩具と同じ遊びです。『どっちにかたむける？』と声をかけると言語化の練習になります」
- **性能規定**: 動的ボディはボール1個のみ・壁は静的なので軽量。60fps目標。壁ボディは面ロード時に一括生成し、プレイ中の生成・削除禁止。

## 4. ゲームB: ころころキャッチ

> **v1.1変更**: 本節の初版（板タップ式・ボール3個・ゴール数スコア）は§1の解釈2のとおり「盤ごと傾けるシーソー型・タイムスコア」に置き換えた。実装（js/games/rollcatch/）が正。以下は初版の記録として残す。

- 画面構成: 縦画面に**シーソー板が4〜5段**、互い違いに配置。最上部からボールが1個ずつ登場し、最下段の下にあるゴール穴を目指す。
- 操作: **板をタップするとその板の傾きが反転**（左下がり⇄右下がり、200msのアニメ）。ボールが乗っている板もタップ可。ドラッグ不要。
- ルール: ボールは板の傾き方向に転がり、板の端から下の段へ落ちる。画面左右の外へ飛び出したらそのボールは終了。1ラウンド=ボール3個。ゴールに入れた数がスコア。
- 難易度:
  - かんたん: **左右に壁があり飛び出さない**（失敗が存在しない。時間がかかるだけ）＋ボール速度ゆっくり
  - ふつう: 壁なし・板4段
  - むずかしい: 壁なし・板5段・ボール速度アップ・板が短い
- 交互スコア対戦: 同一設定で1ラウンドずつ交互に遊び、ゴール数を比較。
- 物理: Matter.js。板=静的ボディを `Body.setAngle` でトゥイーン回転（±12°の2状態）。ボール反発低め。ゴール穴=センサー。
- 演出: ゴールに入ると「ぽとん！ないすキャッチ！」。飛び出しても「おっとっと！つぎいこう！」（ネガ禁止）。
- **知育要素**:
  - **先の板を前もって正しい向きにしておく**と連続で転がる → 2手先を考えるプランニングの体感版。うまく連続したら `smooth_run`（「じゅんびが じょうずだったね！」）
  - タップ→板が傾く→転がる向きが変わる、の即時フィードバック → 因果関係の理解（4歳向けの中核価値）
  - 3個連続ゴール `all_goal`（「ぜんぶキャッチ！すごい！」）
  - 保護者向けヒント: 「ボールが来る前に下の板を直せることに気づくと、遊び方が一段変わります。気づくまで教えずに見守るのがおすすめです」
- **性能規定**: 動的ボディはボール1個（同時1個しか出さない）。板の回転はアニメーション中のみ更新。

## 5. ゲームC: すうじフラッシュ

- ルール（チンパンジーテスト式）:
  1. 画面に数字カード（1〜N）がランダム配置でパッと表示される
  2. 表示時間が過ぎると全カードが「？」に裏返る
  3. **1から順番に**タップ。全部当てたらレベルクリアで数字が1個増える
- レベル進行: 開始N・表示時間は難易度で決定。クリアごとにN+1（上限9）。ミスした瞬間に正解位置を一瞬見せてから、同レベルにもう1回だけ挑戦できる（2回目のミスでラウンド終了）。
  - かんたん: 開始N=3・表示3.5秒
  - ふつう: 開始N=4・表示2.5秒
  - むずかしい: 開始N=5・表示1.5秒
- スコア: 到達した最大N（「すうじ 7こ おぼえられた！」）。ベスト記録保存。
- タップ時に数字を大きく表示＋読み上げ表記（「いち！に！さん！」）→ 数唱とセット。
- 交互スコア対戦: 交互に1ラウンドずつ遊び到達Nを比較（配置はランダムで毎回異なる）。
- 実装: 純DOM（物理・Canvas不要）。カードは最大9枚を事前生成しclass切替で表示/裏返し（DOM生成・削除禁止）。配置は6×4グリッドのセルからランダム抽選（重なりなし・カード間の最小間隔を保証）。
- 演出: 正解タップごとに小さくキラン。レベルクリアで「＋1こ ふえるよ！」。ミス時は「おしい！もういっかい みてみよう」。
- **知育要素**:
  - 視空間ワーキングメモリ（どこに・なんの数字）＋ 数の順序（1→9を必ず順にたどる）の複合。①おぼえる の看板ゲームに位置づける
  - 表示中の「おぼえる時間」は残りをリング表示（焦らせる数字カウントダウンにしない）
  - ほめイベント: 自己ベスト `new_record`、1発クリア `perfect_first_try`、ミス後の再挑戦成功 `comeback`
  - 保護者向けヒント: 「大人より子どものほうが得意なことも多い遊びです。親子対戦で本気で負ける体験が自信につながります」

## 6. データ・アセット追加

```json
// kgb.stats への追加キー
"maze":      { "clears": 4, "plays": 6, "noFallClears": 2 },
"rollcatch": { "best": 3, "plays": 5 },
"flash":     { "best": 7, "plays": 8 }
```

- skills加算: maze→shapeBalance、rollcatch→thinkPower、flash→memoryPower＋numberLetter（こころ⑤は全ゲーム共通+1）。
- 効果音追加: roll（転がり・短ループ）、suck（穴に吸い込まれる）、goal（ゴールイン「ぽとん」）。既存のwin/crash/tapは再利用。
- アセット追加: assets/mazes/（迷路JSON 15面）、ゲームアイコン3種（SVG）。

## 7. 既存文書への差分適用表（Claude Codeが最初に適用すること）

| 対象 | 適用内容 |
|---|---|
| 01_要件定義書 §1 | Doneの定義の「7つのゲーム」→「10のゲーム」 |
| 01_要件定義書 §4.1 | 8〜10番として本書3ゲームを追記、タイトルを「（10本）」に |
| 01_要件定義書 §4.2表 | 上記知育マッピング（§1）を各行に追記 |
| 01_要件定義書 §4.3 | CPU思考なしの例外に3ゲームを追加 |
| 01_要件定義書 §6・§7 | 「7ゲーム」表記→「10ゲーム」 |
| 02_仕様書 §2 | js/games/ に maze/ rollcatch/ flash/ を追加、js/ に motion.js 追加 |
| 02_仕様書 §5 | stats追加キー（本書§6） |
| 02_仕様書 §7 | 効果音3種追加 |
| 02_仕様書 §10 | ロードマップ再編: v0.9 すうじフラッシュ → v0.10 ころころキャッチ → v0.11 ボールめいろ → v0.12 知育まとめ → v0.13 PWA化 → v1.0（すうじ=DOMのみで最軽量、ころころ=Matter.js再利用、めいろ=傾き許可という新規リスクを最後に隔離する順） |
| 両文書の変更ログ | 「v1.3 追加3ゲーム（別冊03参照）」を追記 |

## 8. テスト観点（追加分）

- motion.js: ①許可ダイアログが「はじめる」タップ起点で出る ②拒否→ゆびモードに切り替わる ③机に置いた状態でボールが流れない（キャリブレーション） ④ソファで斜めに持って開始しても遊べる。
- ボールめいろ: 壁抜け・穴の判定もれがないこと（全15面を最低1回クリアする実機テスト）。激しく振ってもボールが壁を貫通しないこと（Matter.jsのタイムステップ固定で対応）。
- すうじフラッシュ: 表示時間中の先行タップを無効化できていること（おぼえる時間はタップ不可）。
- 共通: 3ゲームとも、ゲーム離脱時にセンサー購読・タイマー・rAFが解除されること。

## 9. 変更ログ

| 版 | 日付 | 変更内容 |
|---|---|---|
| v1.0 | 2026-08-30 | 別冊初版。ボールめいろ（端末傾き操作・穴はふつう以上）・ころころキャッチ（板タップ式と解釈）・すうじフラッシュ（チンパンジーテスト式と解釈）の3本を知育設計込みで定義。motion.jsのiOS許可フローとフォールバックを規定 |
| v1.1 | 2026-08-31 | ころころキャッチを駄菓子屋シーソーゲーム型（盤ごと指で傾け・坂/波波で難易度・タイムスコア）に変更（発案者フィードバック。§1解釈2参照） |
