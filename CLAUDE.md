# CLAUDE.md — こどもゲームボックス 開発作法

このリポジトリは、4〜8歳の子ども向けゲーム集（iPhone向けPWA→将来Capacitorでアプリ化）のプロジェクト。
開発者は非エンジニア。**説明は専門用語を初出時のみ一言補足し、過剰な解説はしない。**

## 必ず読む文書

- `01_要件定義書.md` … 何を作るか・やらないこと
- `02_仕様書.md` … どう作るか（ゲームルール・性能ガードレール §9・ロードマップ §10）
- `CHANGELOG.md` … これまでの変更履歴

## 進め方（毎回のセッションで守ること）

1. 最初に「今回の変更目的」と「Doneの定義」を各1行で確定してから着手する。
2. 仕様書のロードマップ（§10）のバージョン単位で小さく作る。1セッションで複数バージョンに手を出さない。
3. 実装前に、性能劣化の芽（ループ内DOM操作、全再描画、毎フレームlocalStorage書き込み、巨大HTML）がないか設計段階で指摘する。
4. コードを出すときは必ず「コード＋バージョン番号＋変更ログ」をセットで出す。
5. 区切りのよいところで「GitHub PagesにデプロイしてiPhone実機で確認」を促す。

## コーディング規約

- 素のHTML/CSS/JS（ES Modules）。フレームワーク・ビルドツールは導入しない（導入したくなったら先に理由を提示して相談）。
- 各ゲームは `js/games/<name>/` に `game.js`（純ロジック・DOM非依存）／`cpu.js`／`ui.js` の3層分離。
- 外部通信コード（fetch・CDN読み込み・アナリティクス）は一切書かない。キッズカテゴリ審査要件のため。
- localStorageの読み書きは `js/storage.js` 経由のみ。直接呼び出し禁止。
- UI文言は `js/i18n.js` に集約。**ひらがな・カタカナのみ**（漢字禁止）。
- 命名: ファイル・変数はcamelCase英語。CSSクラスは `kgb-` プレフィックス＋ケバブケース。
- コメントは日本語でOK。「なぜそうしたか」を書く（whatではなくwhy）。

## 性能ガードレール（仕様書§9の要約・違反コードを書かない）

- 盤面・手札は差分更新。全再描画禁止。
- アニメーションはCSS transform/opacityのみ。
- 画面遷移時にタイマー・リスナーを必ず解除。
- localStorage書き込みはイベント区切りのみ。

## テスト

- `game.js` はNodeで実行できるテスト（`tests/`）を先に通してから `ui.js` に接続する。
- 実行方法: `node tests/<game>.test.js`（テストランナー不使用、console.assertベースで可）。

## バージョンと変更ログ

- バージョンは仕様書§10に従う（v0.1〜v1.0）。ホーム画面フッターに現在バージョンを小さく表示する。
- 変更のたびに `CHANGELOG.md` に追記。粒度は「3か月後の自分が読んで分かる」こと：

```markdown
## v0.3 (2026-09-XX)
- 目的: オセロを実装
- 変更点: js/games/othello/ 追加（game/cpu/ui）、othello.css追加
- 既知の制約: つよいCPUの先読みは2手まで（3手はSEで処理落ちしたため）
- 実機確認: iPhone SE2でフリップアニメ確認済み
```

- Service Workerのキャッシュ名（`kgb-v○○`）もバージョンアップ時に必ず更新する（更新しないと古い画面が出続ける）。

## 禁止事項（子ども向けアプリとして）

- 広告・課金・外部リンク・SNS共有・個人情報収集に類するコードを一切入れない。
- ネガティブな敗北演出（「まけ」「ざんねん」等の強い表現）を使わない。「またあそぼう！」トーンで統一。
