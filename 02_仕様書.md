# 仕様書：こどもゲームボックス（仮称）

- 文書バージョン: v1.3
- 作成日: 2026-08-28（v1.3更新: 同日）
- 対応する要件定義書: 01_要件定義書.md v1.3
- 想定読者: Claude Code（実装担当AI）

> 本書は「フェーズ1: PWA版」の実装仕様。実装は §10 の開発ロードマップの順に、1バージョン=1ゲームの小さな単位で進めること。

---

## 1. 技術構成

| 項目 | 採用技術 | 理由 |
|---|---|---|
| 言語 | HTML / CSS / JavaScript（ES Modules、フレームワークなし） | 非エンジニアが読める最小構成。Capacitor移行が容易 |
| 描画 | DOM＋CSS（盤面・カード類）、Canvas（バランスゲームのみ） | ボード/カードゲームはDOMで十分。物理はCanvas必須 |
| 物理エンジン | Matter.js（バランスゲームのみ、CDNではなくローカル同梱） | オフライン動作要件のため外部CDN禁止 |
| ビルド | なし（素のES Modules）。必要になるまでバンドラ不使用 | ツールチェーン事故を避ける |
| PWA | manifest.json＋Service Worker（キャッシュファースト） | オフライン要件 |
| 保存 | localStorage（設定・戦績のみ。try/catchで全読み書きを保護） | 個人情報ゼロ方針 |
| 音 | Web Audio API。音声ファイルは同梱の短い効果音のみ | BGMなし・ミュート常設 |

### 禁止事項（キッズカテゴリ・性能の観点から実装時に厳守）

- 外部への通信コード（fetch/XHR/アナリティクス/CDN読み込み）を一切書かない。
- 広告SDK・トラッキングコードを入れない。
- ループ内でのDOM書き込み・全盤面再描画をしない（差分更新する。詳細は§9）。

## 2. ファイル構成

```
kids-game-box/
├── CLAUDE.md              # 開発作法（別紙ドラフトあり）
├── CHANGELOG.md           # 変更ログ（バージョンごとに必ず追記）
├── index.html             # ホーム画面（ゲーム選択メニュー）
├── manifest.json
├── sw.js                  # Service Worker
├── css/
│   ├── common.css         # 共通（ボタン、配色トークン、アニメーション）
│   └── games/othello.css など（ゲーム別）
├── js/
│   ├── app.js             # 画面遷移・共通UI制御
│   ├── storage.js         # localStorageラッパー（try/catch集約）
│   ├── sound.js           # 効果音・ミュート管理
│   ├── i18n.js            # UI文言定義（ひらがな文言・ほめ言葉を一元管理）
│   ├── praise.js          # 具体ほめシステム（イベント受取→結果画面用の文言選定）
│   ├── stamps.js          # スタンプちょう
│   ├── parent.js          # ペアレンタルゲート＋保護者画面（せいちょうきろく）
│   └── games/
│       ├── othello/  （game.js=ルール・状態, cpu.js=思考, ui.js=描画）
│       ├── sevens/
│       ├── memory/
│       ├── oldmaid/
│       ├── balance/
│       ├── tictactoe/
│       └── mole/     （もぐらたたき。CPUなしのため game.js=進行・スコア, ui.js の2ファイル）
├── lib/matter.min.js      # ローカル同梱
└── assets/
    ├── icons/             # アプリアイコン・ゲームアイコン(SVG)
    ├── cards/             # トランプ絵柄(SVG、数字＋マーク＋色で判別)
    └── sounds/            # 効果音(短いmp3/ogg)
```

**設計原則**: 各ゲームは `game.js`（純ロジック・DOM非依存）／`cpu.js`（思考）／`ui.js`（描画・入力）の3層に分離する。ロジックをDOMから切り離すことで、Claude Codeによるテスト（Node実行）が可能になる。

## 3. 共通UI仕様

### 3.1 画面遷移

```
ホーム（7ゲームのアイコンが並ぶ。2列グリッド）
 └→ ゲーム別設定画面（あそびかた選択: CPUとあそぶ／ふたりであそぶ、つよさ選択）
     └→ ゲーム画面
         └→ 結果画面（もういちど／ホームへ）
```

- 遷移はSPA方式（1つのindex.htmlで画面div切り替え）。ページリロードなし。
- 戻る操作: 各画面左上に「←」ボタン。ゲーム中は「やめる？」確認ダイアログを挟む（誤タップ対策）。

### 3.2 デザイントークン（common.cssで定義）

- 配色: 明るく彩度高めのパステル基調。背景クリーム色、ゲームごとにテーマ色を1色。
- 文字: システムフォント（-apple-system）。最小フォントサイズ20px。全文言ひらがな・カタカナ。
- ボタン: 最小60×60px、角丸、押下時に軽く縮むアニメーション＋効果音。
- 手番表示: 画面上部に「○○のばん」を大きく表示し、該当プレイヤー色で点滅囲み。

### 3.3 共通機能

- ミュートボタン（右上常設、状態をlocalStorage保存）
- 交互プレイの手番交代時は「かわって！」オーバーレイを表示し、タップで再開（手札が見えるゲームでは必須）
- 勝敗演出: 勝ち=紙吹雪アニメ＋ファンファーレ、負け=「またあそぼう！」（ネガティブ表現禁止）
- 戦績: ゲームごとの勝ち数のみ記録・表示（星の数で表現）。リセットは設定から

### 3.4 知育共通機能（要件定義書§4.2「5つのあそびのちから」に対応）

1. **具体ほめシステム（praise.js）**: ゲーム側がプレイ中の行動イベント（例: `remembered_pair`, `took_corner`, `retried`, `waited_turn`, `finished_game`）を発火し、結果画面で勝敗と別枠の「きょうのすごいところ」として1つ表示する。文言はi18n.jsに集約し、同じほめ言葉が連続しないようローテーション。勝敗に関係なく必ず1つはほめる。
2. **スタンプちょう**: 1プレイ完了で1スタンプ（ゲーム別の絵柄）。ホームから閲覧。ログインボーナス・期限・コンプ報酬なし。10個ごとに小さなお祝いアニメのみ。
3. **せいちょうきろく（保護者向け画面）**:
   - 入口: ホーム右下の小さな「おうちのかたへ」ボタン → ペアレンタルゲート（「3+4=?」等の簡単な計算を数字キーで入力。不正解は静かに閉じる）→ 保護者画面。
   - 内容: ①5つの力ごとの遊んだ回数（レーダー等の凝ったグラフは不要、星の数で十分） ②ゲームごとの「声かけヒント」（例: オセロ「『どうしてそこに置いたの？』と聞くと考えを言葉にする練習になります」） ③「ほんもので遊ぶコツ」（実物トランプ・オセロへの橋渡し） ④きゅうけいリマインダー設定。
   - すべてアプリ内テキストで完結。外部リンクなし。
4. **きゅうけいリマインダー**: 連続プレイ15分/30分/なし（初期値30分）。到達でゲームの区切り（1局終了時）に「そろそろきゅうけいしよう！」全画面表示。ゲーム途中に割り込まない。
5. **難易度アシスト**: 「よわい」CPUは同一ゲームで2連敗を検知したら、次のプレイでミス率を上げる（実装はcpu.jsのパラメータ切替のみ。特別な演出はしない）。

> 実装上の位置づけ: praise.js／stamps.js／parent.js を js/ 直下に置き、各ゲームからはイベント発火のみ行う（ゲームロジックに知育コードを混ぜない）。

## 4. ゲーム別仕様

### 4.1 オセロ

- 盤面: 8×8。初期配置は中央4石。黒先手。
- 石: 黒＝くろネコ顔／白＝しろネコ顔など、色＋絵柄で判別（色覚対応）。※アイコンは仮。実装時はシンプルな円＋マークで開始
- **ルール補助**: 置けるマスを常時ハイライト（点滅ドット）。置けない場所をタップしたら「ぶぶー」音＋盤が小さく揺れる。
- パス: 置ける場所がない場合は自動パス（「パス！」表示1.5秒）。両者連続パスまたは盤面満杯で終局。
- ひっくり返し: 1枚ずつ順にフリップアニメーション（80ms間隔）。アニメ中は入力ロック。
- CPU思考（cpu.js）:
  - よわい: 合法手からランダム。ただし角が取れるときも50%で見逃す
  - ふつう: その手で返せる枚数最大を選ぶ（貪欲法）。角があれば必ず取る
  - つよい: 位置重み評価（角100、角隣接−20、辺10など）＋2手先読みミニマックス
  - 思考時間演出: 600〜1200msのランダム待機後に着手（即打ちは子どもが混乱するため）
- 終局: 石数を数え上げアニメーション→勝敗表示。
- **知育要素（②かんがえる・③かず・④かたち）**:
  - 「かずのせんせい」設定（ON時）: 置ける場所ハイライトに「返せる枚数」を数字で表示（数量比較の練習。つよいCPU戦ではOFF推奨の旨を保護者画面に記載）
  - 終局の数え上げは1個ずつ「1、2、3…」と数字を大きく表示しながら数える（数唱の練習。タップでスキップ可）
  - ゲーム開始前に1枚だけ「ワンポイント」カード（例: 「かどをとるとつよいよ！」全6種ローテーション）→ 作戦という概念の導入
  - ほめイベント: 角を取った `took_corner`、逆転した `comeback`、最後まで打ち切った `finished_game`

### 4.2 7並べ

- カード: ジョーカーなし52枚。7を場に自動配置してスタート。
- 人数: プレイヤー vs ロボット1〜3たい（2〜4人戦、設定画面で選択）、または2人交互。（実装済み）
- 手札表示: 自分の手札を画面下に扇状表示。出せるカードを光らせる（ルール補助）。CPU/相手の手札は枚数のみ。
- パス: 3回まで。残パス数をハート図柄で表示。パス超過はその人だけ**リタイア**（手札を場に開いてゲームから抜ける）。残りの人で続行し、先に出し切った人か最後まで残った人が勝ち。（実装済み）
- 場: 4スート×13の格子。出せる位置は7から連続する隣のみ。カードは出せる位置に自動配置（ドラッグ不要、手札タップのみ）。
- CPU思考:
  - よわい: 出せるカードからランダム。パスをほぼ使わない
  - ふつう: 端(A/K)に近いカードを優先して出す
  - つよい: 相手を止められるカード（止め札）を温存する戦略＋パスを戦略的に使用
- 交互プレイ時: 手番交代オーバーレイ必須（手札が見えるため）。
- **知育要素（②かんがえる・③かず）**:
  - 場は**数直線そのもの**として扱う: カードを出した瞬間、そのスートの列で「6のとなりは7」のように隣接数字が一瞬光る（数の順序の可視化）
  - カードを出すとき中央に数字を大きく表示（「8！」）。A/J/Q/Kは「1」「11」「12」「13」を併記（記号と数の対応）
  - 手札の出せるカード発光は「なぜ出せるか」（場の端の隣だから）を線でつなぐ簡易表示
  - ほめイベント: パスを我慢して考えた `thought_long`（5秒以上考えて着手）、止め札に気づいた表示はしない（高度すぎるため将来検討）

### 4.3 神経衰弱

- カード枚数（難易度）: かんたん=8枚(4組)／ふつう=16枚(8組)／むずかしい=24枚(12組)。
- 絵柄: 動物・果物などのSVGイラスト。トランプ数字ではなく絵合わせ（4歳対応）。
- ルール: 2枚めくって一致→取得＋もう1回。不一致→1秒表示後に裏返し手番交代。
- CPU思考（記憶精度で難易度表現）:
  - よわい: 直近2枚だけ記憶、それ以外はランダム
  - ふつう: めくられたカードを70%の確率で記憶
  - つよい: 全て記憶（既知ペアがあれば必ず取る）
- ひとりプレイ: 全部そろえるまでのめくり回数を記録（すくないほどすごい）。
- 演出: 一致時にカードがジャンプ＋効果音。
- **知育要素（①おぼえる・③かず・もじ）— 本ゲームは絵柄テーマ切替で知育の幅を出す**:
  - テーマ「どうぶつ」「くだもの」: 絵と絵のマッチング。めくった瞬間に名前をひらがな・カタカナ表示（「ぞう」「ライオン」）→ 語彙
  - **絵柄リスト（確定・SVG自作、各8種）**:
    - どうぶつ: ライオン、ぞう、キリン、いぬ、ねこ、さる、ぱんだ、コアラ
    - くだもの: りんご、みかん、ぶどう、いちご、バナナ、スイカ、メロン、もも
    - むずかしい（12組）は1テーマ8種では足りないため、どうぶつ＋くだもの混合16種から12種を使用
  - テーマ「すうじ」: 数字カード「3」とドット絵カード「●●●」のマッチング → 数字と数量の対応（v0.9で追加）
  - テーマ「ひらがな」: 絵カード「りんごの絵」と文字カード「りんご」のマッチング → 文字への興味（v0.9で追加）
  - ほめイベント: 2回目で当てた `remembered_pair`、連続一致 `combo`、全部めくり切った `finished_game`

### 4.4 ばば抜き

- 人数: プレイヤー＋CPU2人の3人戦を基本（2人交互モードは2人戦）。
- カード: 26枚(2〜7のペア相当)に減らした短時間版を「かんたん」、53枚(ジョーカー1枚)を「ふつう／つよい」とする。※4歳の集中時間対策
- 配札後、初期ペアは自動で捨てる（アニメーションで見せる）。
- 引く操作: 隣のプレイヤーの裏向きカードが扇状に表示され、1枚タップで引く。ペア成立時は自動で捨てる＋効果音。
- CPU思考:
  - よわい: ランダムに引く。ジョーカーを引いても表情変化なし
  - ふつう: ランダム＋引かれるカードの並び替えなし
  - つよい: ジョーカーの位置をシャッフルして引かせにくくする演出
- 上がり順を表示（1ぬけ・2ぬけ）。ジョーカーを最後まで持っていた人が負けだが「ばばもってたのは○○！またあそぼう！」の軽い演出に留める。
- **知育要素（①おぼえる・③かず・⑤こころ）**:
  - 「おなじさがし」補助（ON時）: カードを引いた瞬間、手札に同じ数字があれば両方が薄く点滅（同数マッチング＝ペア概念の学習。OFFで自力探し）
  - 「おかたづけ」設定: 手札の自動整列をOFFにすると、配札後に自分でカードをタップ入替えで並べ替えられる（分類・整理の練習。初期値は自動整列ON）
  - 引く前のドキドキ演出（カードが少し揺れる）で「我慢して選ぶ」体験を演出
  - ほめイベント: ペアを自力で見つけた `found_pair`、最後まで諦めなかった `finished_game`、順番を正しく待てたのは全ゲーム共通の `waited_turn`

### 4.5 バランスゲーム（積み上げ）

- エンジン: Matter.js。Canvas描画。
- ルール: 画面下の土台の上に、上から降ってくる形の違うブロック（長方形・L字・丸など）を左右移動＋タップで落として積む。土台から1個でも画面外に落ちたらゲーム終了。
- 操作: 画面左右半分タップで移動、下スワイプor中央タップで落下。ドラッグ不要。
- スコア: 積めた個数。ベスト記録をlocalStorage保存。
- 交互対戦モード: 1個ずつ交互に積み、崩した方が負け。
- 難易度: ブロックの形状セットで調整（かんたん=四角のみ／ふつう=四角＋丸／むずかしい=L字・三角混在）。
- 性能規定: ブロック上限30個。30個到達で「すごい！」クリア演出。静止したブロックはスリープ設定（Matter.jsのsleeping有効化）でCPU負荷を抑える。
- **知育要素（④かたち・⑤こころ）**:
  - 落ちてくるブロックに形の名前をひらがな表示（「さんかく」「ましかく」「まる」）→ 図形語彙
  - 崩れたとき、直前の塔に重心マーカー（⭐）を1秒表示し「まんなかにのせるとつよいよ」と一言 → 重さ・バランスの直感を言語化（物理用語は使わない）
  - リトライ導線を最短に（崩れる→ワンタップで再開始）し、試行錯誤のリズムを守る
  - ほめイベント: 自己ベスト更新 `new_record`、3回以上連続チャレンジ `retried`（「なんかいもチャレンジしてえらい！」）

### 4.6 ◯×ゲーム（三目並べ）

- 盤面: 3×3。プレイヤー=◯（先手）、CPU/相手=×。2回戦目以降は先手交代。
- マスをタップで着手。置き済みマスはタップ無効（buzzer音なし、無反応でよい）。
- 勝敗: 縦横斜めの3並びで勝ち。成立ラインに光るアニメーション。全マス埋まりは「ひきわけ！ふたりともつよい！」（引き分けをポジティブに扱う）。
- CPU思考:
  - よわい: 合法手からランダム
  - ふつう: ①自分が勝てる手→②相手の勝ちを防ぐ手→③ランダム、の優先順
  - つよい: ミニマックス完全読み。ただし初手・2手目のみランダム性を入れ、毎回同じ展開を避ける
- 1ゲームが短いため「もういちど」を最短導線に（結果画面から1タップで再戦、先手交代）。
- **知育要素（②かんがえる・⑤こころ）**:
  - 「リーチ」表示（ON時）: 自分または相手が2つ並んだラインを薄く点滅させ「あとひとつ！」→ パターン認識と「防ぐ」概念の入門。オセロの先読みへの階段として位置づける
  - ほめイベント: 相手のリーチを防いだ `blocked_reach`、ひきわけ `draw_positive`、先手後手どちらでも最後まで打った `finished_game`
  - 保護者向けヒント: 「『どこに置いたら まけない？』と一緒に考えると先読みの練習になります」

### 4.7 もぐらたたき

- 形式: ひとりプレイ＋交互スコア対戦（同一設定で1ラウンドずつ遊びスコア比較）。**CPU対戦なし（cpu.js を作らない）**。
- 画面: 3×3の穴。1ラウンド30秒。残り時間はプログレスバー（数字カウントダウンは焦らせるため補助的に小さく）。
- 出現物:
  - もぐら: タップで+1。ヒットで「ぽこっ」演出＋スコアが1ずつ大きく表示（数唱）
  - ちょうちょ: **たたかない対象**（go/no-go課題＝抑制制御）。たたくとスコアは増えず「あっ、ちょうちょさん！」表示＋コンボが切れる。減点はしない（4歳への配慮）
- 難易度（かんたん／ふつう／むずかしい）: 出現間隔 1200/900/650ms、同時最大 1/2/3体、ちょうちょ出現率 0%/15%/25%。かんたんはちょうちょなし（純粋な協応運動）。
- コンボ: 連続ヒットで「◯こつづけて！」表示。ちょうちょヒット・空振りでリセット。
- 終了: 「きょうは 12ひき たたけたね！」→ 自己ベスト更新で `new_record`。
- **実装の性能規定（重要）**:
  - 判定は `pointerdown` で行う（clickより体感が速い）。**マルチタッチ対応**（両手たたき）のため touch-action: manipulation を指定
  - もぐら・ちょうちょのDOMノードは9穴ぶん事前生成し、class切替（`.up` / `.down` / `.hit`）だけで出し入れする。**プレイ中のDOM生成・削除は禁止**
  - アニメーションはCSS transform（translateY）のみ。出現スケジューラは単一のsetIntervalで管理し、ラウンド終了時に必ずclear
- **知育要素（③かず・④かたち・⑤こころ）**:
  - 手と目の協応＋「たたいていいもの／だめなもの」を見分けてがまんする抑制制御（⑤の中核ゲーム）
  - スコアの数唱表示、終了時の「◯ひき」助数詞つき読み上げ表示
  - ほめイベント: ちょうちょを1回もたたかなかった `perfect_patience`（「ちょうちょさんを まもれたね！」）、自己ベスト `new_record`

## 5. データ構造（localStorage）

キーは `kgb.` プレフィックスで統一。保存は settings と stats の2キーのみ。

```json
// kgb.stats
{
  "othello":  { "wins": 3, "plays": 10 },
  "sevens":   { "wins": 1, "plays": 4 },
  "memory":   { "wins": 2, "plays": 6, "bestMoves": { "easy": 10, "normal": 24 } },
  "oldmaid":  { "wins": 0, "plays": 2 },
  "balance":  { "best": 12, "plays": 8 },
  "tictactoe": { "wins": 5, "plays": 9 },
  "mole":     { "best": 14, "plays": 6 },
  "skills":   { "memoryPower": 8, "thinkPower": 14, "numberLetter": 6, "shapeBalance": 8, "heartPower": 30 },
  "stamps":   30,
  "lossStreak": { "othello": 0 },
  "version": 1
}

// kgb.settings
{ "muted": false, "breakMinutes": 30, "assist": { "othelloCount": true, "oldmaidPairHint": true, "oldmaidAutoSort": true }, "version": 1 }
```

- skills（5つの力カウンタ）は1プレイ終了時に担当ゲームの力へ+1（こころのちから⑤は全ゲームで+1）。保護者画面の星表示の元データ。
- lossStreakは難易度アシスト用（連敗検知）。ゲームごとに保持し勝利でリセット。

- 読み書きは必ず storage.js 経由。パース失敗・容量超過時は初期値で続行（ゲームを止めない）。
- versionフィールドは将来のマイグレーション用。

## 6. PWA仕様

- manifest.json: `display: "standalone"`、`orientation: "portrait"`、テーマカラー、アイコン(192/512px)。
- sw.js: インストール時に全アセットをプリキャッシュ（キャッシュファースト戦略）。キャッシュ名にバージョン番号を含め、更新時に旧キャッシュ削除。
- ホスティング: GitHub Pages（無料・HTTPS）。無料プランのPagesはpublicリポジトリ限定のため、リポジトリはpublicで運用する（外部通信ゼロ方針のためコードに秘匿情報は存在しない）。

## 7. 効果音一覧（同梱・自作またはCC0素材）

tap（ボタン）、place（石・カードを置く）、flip（めくる）、match（一致・ペア）、buzzer（無効操作）、win（ファンファーレ）、turn（手番交代）、fall（ブロック落下）、crash（崩れる）、pop（もぐら出現）、bonk（もぐらヒット）。各1秒未満、合計600KB以下。

## 8. テスト方針

- 各ゲームの game.js（純ロジック）にはClaude Codeがテストコードを書き、Nodeで実行して合格させてから ui.js に接続する（例: オセロの合法手判定・全反転パターン、7並べの出せる判定）。
- 実機テスト: 各バージョン完了ごとにGitHub Pagesへデプロイし、iPhone実機で確認する。**確認観点チェックリストをCHANGELOGに添付**（タップ反応、アニメ滑らかさ、文言の読みやすさ、子どもの実プレイ観察）。
- 性能テスト: バランスゲームはiPhone SEクラスでfps表示デバッグモード（URLに `?debug` で有効化）を用意。

## 9. 性能ガードレール（実装前に必ず守る）

1. **全再描画禁止**: 盤面・手札は差分更新（変わったセル/カードのDOMだけ触る）。オセロで毎手64マス再生成しない。
2. **ループ内DOM操作禁止**: 複数要素の更新はDocumentFragmentか一括class切替で。
3. **アニメはCSS transform/opacityのみ**: left/top/width操作はレイアウト再計算を招くため禁止。
4. **タイマー管理**: 画面遷移時に必ずclearInterval/cancelAnimationFrame。リスナーはAbortControllerで一括解除。
5. **localStorage書き込みはイベント区切りのみ**（ゲーム終了時・設定変更時）。毎フレーム書き込み禁止。
6. **画像はSVG優先**、ラスタは@2xまで。1ファイル100KB超を作らない。

## 10. 開発ロードマップ（1バージョン=1リリース単位、各回CHANGELOG必須）

> v1.2文書のロードマップは新規開発前提の番号だったが、v0.1〜v0.4.1が旧5ゲーム構成で完了済みのため、
> 実績を保ったまま以下に再編した（済のバージョン番号は付け替えない）。

| 版 | 内容 | 状態 / Doneの定義 |
|---|---|---|
| v0.1 | 共通シェル（ホーム・画面遷移・デザイントークン・効果音基盤・storage.js） | ✅ 完了 (2026-08-28) |
| v0.2 | 神経衰弱（ひとり・ロボット・ふたり） | ✅ 完了 (2026-08-28) |
| v0.3 | オセロ（ロボット3段階＋ふたり） | ✅ 完了 (2026-08-28) |
| v0.4 | 7ならべ（v0.4.1でロボット1〜3たい・リタイア制に拡張） | ✅ 完了 (2026-08-28) |
| v0.5 | ◯×ゲーム＋ほめシステム骨格（praise.js・plays/5つの力カウンタの記録開始）＋ホーム7ゲーム2列化 | ロボット3段階＋ふたりが遊べる。ロジックテスト合格。結果画面に「きょうのすごいところ」が出る |
| v0.6 | もぐらたたき（タイマー・アニメーションの基礎確立） | かんたん〜むずかしい＋こうたい対戦が実機で快適 |
| v0.7 | ばば抜き | ロボット戦(3人戦)＋ふたりが実機で遊べる。ロジックテスト合格 |
| v0.8 | バランスゲーム | iPhone SE相当で30fps以上を確認 |
| v0.9 | 知育まとめ（スタンプちょう・保護者画面「せいちょうきろく」・きゅうけいリマインダー・難易度アシスト・神経衰弱の「すうじ」「ひらがな」テーマ、**実装済みゲームへの知育要素レトロフィット**: オセロ数え上げ数唱/かずのせんせい/ワンポイント、7ならべ数直線演出、神経衰弱なまえ表示） | 保護者画面・スタンプが実機で動作。5つの力カウンタが記録される |
| v0.10 | PWA化（manifest＋SW）・アイコン・仕上げ（※0.9の次は0.10。二桁でOK） | オフラインで全ゲーム動作 |
| v1.0 | 総合テスト・βテスト反映 | 要件定義書§7の成功基準を満たす |

> v0.5以降の新ゲームは、そのゲーム固有の知育機能（リーチ表示・ほめイベント等）を同バージョンに含めて実装する。v0.9は横断機能と既存ゲームのレトロフィットのまとめ回。

> フェーズ2（Capacitor化・App Store提出）は v1.0 完了後に別仕様書を起こす。

## 11. 変更ログ

| 版 | 日付 | 変更内容 |
|---|---|---|
| v1.0 | 2026-08-28 | 初版。5ゲームのルール・CPU思考・データ構造・性能ガードレール・ロードマップを定義 |
| v1.1 | 2026-08-28 | 知育要素を追加: §3.4知育共通機能（ほめ・スタンプ・保護者画面・きゅうけい・難易度アシスト）、各ゲームに知育要素ブロック、stats/settingsに知育データ追加、ロードマップをv0.8構成に変更（v0.7=知育まとめ回） |
| v1.2 | 2026-08-28 | ◯×ゲーム（§4.6）・もぐらたたき（§4.7）を追加し7ゲーム構成に。神経衰弱の絵柄16種を確定（どうぶつ8＋くだもの8、むずかしいは混合）。効果音にpop/bonk追加。ロードマップをv0.10構成に再編（◯×→もぐら→神経衰弱の順に変更） |
| v1.3 | 2026-08-28 | 実装実績との整合: §4.2を多人数(ロボット1〜3)・リタイア制の実装済み仕様に更新、§6をpublicリポジトリ運用に修正、§10を実績ベース（v0.1〜v0.4完了・v0.5=◯×ゲーム）に再編 |
